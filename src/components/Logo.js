import {StyleSheet, Text, View} from 'react-native';
import React from 'react';

const Logo = () => {
  return (
    <View>
      <Text>Logo</Text>
    </View>
  );
};

export default Logo;

const styles = StyleSheet.create({});
